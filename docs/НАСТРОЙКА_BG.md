# 🇧🇬 Ръководство за Настройка на Wallestars Control Center

## 📍 Къде да поставите `.env` файла?

`.env` файлът трябва да се намира в **главната (root) директория** на проекта, на същото ниво като `package.json`.

```
Wallestars/
├── .env                    👈 ПОСТАВЕТЕ ТУК!
├── .env.example           (шаблон)
├── package.json
├── README.md
├── server/
└── src/
```

## 🔧 Как да създадете `.env` файл?

### Метод 1: Копиране на примера

```bash
# В главната директория на проекта
cp .env.example .env
```

След това отворете `.env` и добавете вашите настройки:

```env
# Anthropic API Configuration
ANTHROPIC_API_KEY=sk-ant-api03-вашия-ключ-тук

# Server Configuration
PORT=3000
NODE_ENV=development

# Linux Computer Use
ENABLE_COMPUTER_USE=true
SCREENSHOT_INTERVAL=2000

# Android ADB Configuration
ADB_HOST=localhost
ADB_PORT=5037
ENABLE_ANDROID=true

# WebSocket Configuration
WS_PORT=3001
```

### Метод 2: Създаване на нов файл

1. **Linux/Mac:**
   ```bash
   nano .env
   # или
   vi .env
   # или
   code .env  # ако използвате VS Code
   ```

2. **Windows:**
   ```bash
   notepad .env
   # или
   code .env  # ако използвате VS Code
   ```

## 🚨 Проблем: "Портовете не работят"

### Причини за проблема:

#### 1. ❌ Липсващ `.env` файл
**Решение:**
- Създайте `.env` файл в главната директория (виж горе)
- Добавете вашия `ANTHROPIC_API_KEY`

#### 2. ❌ Неправилен API ключ
**Проверка:**
```bash
# Проверете дали ключът е зададен
cat .env | grep ANTHROPIC_API_KEY
```

**Решение:**
- Уверете се, че ключът започва с `sk-ant-`
- Получете нов ключ от: https://console.anthropic.com

#### 3. ❌ Портовете са заети
**Проверка:**
```bash
# Linux/Mac - проверете дали порт 3000 е зает
lsof -i :3000

# Linux/Mac - проверете дали порт 5173 е зает (Vite)
lsof -i :5173

# Windows - проверете портове
netstat -ano | findstr :3000
netstat -ano | findstr :5173
```

**Решение:**
```bash
# Спрете процеса на заетия порт (Linux/Mac)
kill -9 <PID>

# Или сменете порта в .env
PORT=3001  # вместо 3000
```

#### 4. ❌ Node модулите не са инсталирани
**Решение:**
```bash
# Инсталирайте dependencies
npm install

# Ако има проблеми, изчистете кеша
rm -rf node_modules package-lock.json
npm install
```

## ✅ Правилна Последователност за Стартиране

### 1. Проверка на настройките

```bash
# Проверете дали .env файлът съществува
ls -la .env

# Проверете съдържанието (без да показвате ключа!)
cat .env | grep -v "API_KEY"
```

### 2. Инсталиране на зависимости

```bash
npm install
```

### 3. Стартиране на development сървър

```bash
# Стартира и frontend (Vite на порт 5173) и backend (Express на порт 3000)
npm run dev
```

**Очаквайте да видите:**
```
╔═══════════════════════════════════════════════════════╗
║                                                       ║
║   🌟 WALLESTARS CONTROL CENTER 🌟                    ║
║                                                       ║
║   Server running on: http://localhost:3000            ║
║   WebSocket ready on: ws://localhost:3000             ║
║                                                       ║
║   Services Status:                                    ║
║   ✅ Claude API                                       ║
║   ✅ Computer Use (Linux)                             ║
║   ✅ Android Control                                  ║
║                                                       ║
╚═══════════════════════════════════════════════════════╝
```

### 4. Отваряне в браузър

```
http://localhost:5173
```

## 🔍 Диагностика на Проблеми

### Тест 1: Проверка на Backend

```bash
# Отворете нов терминал и тествайте health endpoint
curl http://localhost:3000/api/health
```

**Очакван резултат:**
```json
{
  "status": "healthy",
  "timestamp": "2024-01-03T11:22:34.563Z",
  "services": {
    "claude": true,
    "computerUse": true,
    "android": true
  }
}
```

### Тест 2: Проверка на Frontend

Отворете браузър на:
```
http://localhost:5173
```

Трябва да видите Dashboard-а на Wallestars Control Center.

### Тест 3: Проверка на WebSocket

```bash
# Инсталирайте wscat ако нямате
npm install -g wscat

# Тествайте WebSocket връзката
wscat -c ws://localhost:3000
```

## 📱 Специфични Настройки

### За Linux Computer Use

```bash
# Инсталирайте необходимите инструменти
sudo apt update
sudo apt install -y xdotool scrot

# Проверка
which xdotool
which scrot
```

### За Android Control

```bash
# Инсталирайте ADB
sudo apt install -y android-tools-adb

# Проверка на устройства
adb devices

# Стартирайте ADB server ако не е стартиран
adb start-server
```

## 🐳 Алтернатива: Docker

Ако имате проблеми с локалната настройка, използвайте Docker:

```bash
# Създайте .env файл (както е описано по-горе)

# Стартирайте с Docker Compose
docker compose up -d --build

# Проверете логове
docker compose logs -f

# Спрете контейнерите
docker compose down
```

## 🆘 Често Срещани Грешки

### Грешка: "Cannot find module 'dotenv'"

```bash
npm install dotenv
```

### Грешка: "ANTHROPIC_API_KEY is not defined"

```bash
# Уверете се, че .env файлът съществува
ls .env

# Проверете дали ключът е зададен
cat .env | grep ANTHROPIC_API_KEY

# Рестартирайте сървъра след промени в .env
# Ctrl+C за да спрете, после:
npm run dev
```

### Грешка: "Port 3000 is already in use"

```bash
# Намерете процеса
lsof -i :3000

# Спрете процеса
kill -9 <PID>

# Или сменете порта в .env
PORT=3001
```

### Грешка: "Failed to connect to WebSocket"

1. Уверете се, че backend сървърът е стартиран
2. Проверете дали портът 3000 е достъпен
3. Проверете firewall настройките

```bash
# Linux - проверете firewall
sudo ufw status

# Ако е необходимо, отворете порта
sudo ufw allow 3000/tcp
```

## 📞 Допълнителна Помощ

Ако все още имате проблеми:

1. **Проверете логовете:**
   ```bash
   npm run dev 2>&1 | tee logs.txt
   ```

2. **Споделете информация за грешката:**
   - Съдържание на `.env` (БЕЗ API ключа!)
   - Съобщения за грешки от конзолата
   - Версия на Node.js: `node --version`
   - Операционна система

3. **Рестартирайте всичко:**
   ```bash
   # Спрете всички процеси (Ctrl+C)
   
   # Изчистете
   rm -rf node_modules dist
   
   # Преинсталирайте
   npm install
   
   # Стартирайте отново
   npm run dev
   ```

## ✨ Успешна Настройка!

Когато всичко работи правилно, ще видите:

1. ✅ Backend сървър на `http://localhost:3000`
2. ✅ Frontend приложение на `http://localhost:5173`
3. ✅ WebSocket връзка активна
4. ✅ Claude API готов за използване
5. ✅ Computer Use функции достъпни (Linux)
6. ✅ Android Control готов (при свързано устройство)

**Готово за използване! 🚀**
