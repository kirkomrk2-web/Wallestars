# 🦎 Сигурен отдалечен достъп с Tailscale

Tailscale е препоръчителният начин за сигурен достъп до вашия Wallestars Control Center от всяка точка на света, без да излагате сървъра си на публичния интернет и без сложни настройки за пренасочване на портове (port forwarding).

## 🛡️ Какво е Tailscale?

Tailscale е VPN без нужда от сложна конфигурация (zero-configuration), който създава сигурна частна мрежа между вашите устройства. Той се инсталира на вашия сървър (VPS или домашен компютър) и на вашите клиентски устройства (телефон, лаптоп, таблет), позволявайки им да комуникират помежду си така, сякаш са в една и съща Wi-Fi мрежа, независимо къде се намират.

### Защо да използвате Tailscale с Wallestars?

- **Сигурен Достъп**: Достъпвайте таблото си за управление през 4G/5G или други Wi-Fi мрежи напълно защитено.
- **Без Отворени Портове**: Не е нужно да отваряте портове 3000 или 80 към публичния интернет, което повишава сигурността.
- **Лесна Настройка**: Отнема по-малко от 5 минути.
- **MagicDNS**: Достъпвайте сървъра си по име (напр. `http://wallestars-server`) вместо по IP адрес.

---

## 🚀 Ръководство за Инсталация

### Стъпка 1: Създайте Tailscale акаунт

1. Отидете на [tailscale.com](https://tailscale.com).
2. Регистрирайте се безплатно (Планът "Personal" е безплатен за до 3 потребители и 100 устройства).

### Стъпка 2: Инсталирайте на вашия Сървър (Wallestars Host)

Ние сме подготвили помощен скрипт, за да улесним процеса:

```bash
# Стартирайте скрипта за инсталация
./setup-tailscale.sh
```

**Ръчна Инсталация:**
Ако предпочитате да инсталирате ръчно:

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up
```

Следвайте линка, отпечатан в конзолата, за да удостоверите сървъра и да го добавите към вашата мрежа (tailnet).

### Стъпка 3: Инсталирайте на вашите Устройства

Изтеглете приложението Tailscale за вашето устройство:
- **Android**: [Google Play Store](https://play.google.com/store/apps/details?id=com.tailscale.ipn)
- **iOS**: [App Store](https://apps.apple.com/us/app/tailscale/id1470499037)
- **Windows/macOS**: [Страница за изтегляне](https://tailscale.com/download)

Влезте със същия акаунт, който използвахте в Стъпка 1.

---

## 🔗 Свързване с Wallestars

След като и сървърът, и клиентското устройство (напр. телефон) са в Tailscale мрежата:

1. Отворете приложението Tailscale на телефона си.
2. Намерете вашия сървър в списъка с устройства.
3. Копирайте неговия **Tailscale IP адрес** (напр. `100.x.y.z`).
4. Отворете браузъра и въведете:
   ```
   http://100.x.y.z:3000
   ```
   (Или само `http://100.x.y.z`, ако имате конфигуриран Nginx на порт 80).

### Използване на MagicDNS

Ако сте активирали MagicDNS в административната конзола на Tailscale, можете да достъпвате сървъра си директно по неговото име:

```
http://wallestars-vps:3000
```

---

## 🌟 Възможности на Платформата (Advanced Features)

Tailscale предоставя и допълнителни възможности, които могат да бъдат полезни:

### Exit Nodes (Изходни Възли)
Можете да конфигурирате вашия Wallestars сървър като "Exit Node". Това ви позволява да пренасочвате *целия* си интернет трафик от телефона през вашия сигурен домашен/VPS сървър. Това е идеално решение, когато използвате незащитени публични Wi-Fi мрежи (напр. в кафенета или хотели), тъй като криптира връзката ви.

**За активиране на сървъра:**
```bash
sudo tailscale up --advertise-exit-node
```
След това одобрете настройката в [Tailscale Admin Console](https://login.tailscale.com/admin/machines). В приложението на телефона изберете "Use Exit Node".

### Taildrop
Лесно и сигурно изпращане на файлове между вашите устройства. Например, можете да изпратите логове или конфигурационни файлове от сървъра към лаптопа си без нужда от USB памети или качване в облака.

### ACLs (Контрол на достъпа)
Ако споделяте сървъра с други хора, можете да използвате Access Control Lists (ACLs), за да ограничите кой до какво има достъп в мрежата.
